# Day 1 复盘（2026-08-27 周四）

1. **今天学了什么**：Console 的 Write / WriteLine 区别（Write 不换行，WriteLine 换行）；ReadLine / Read / ReadKey 区别（读一整行 / 读单个字符 / 读单个按键）；五种基础类型 int / double / bool / char / string；ReadKey 在 F5 调试下会报错，要用终端 `dotnet run` 运行。
2. **卡在哪了**：`Console.ReadKey()` 在 F5 调试运行时抛 InvalidOperationException（"console input has been redirected"，键盘输入被重定向），一度不知原因；改用终端运行后解决。
3. **明天第一步**：学运算符 / 表达式 / 类型转换，写计算器雏形（加减乘除函数）；LeetCode 第一题"两数之和"（用 C# 实现）。

## 代码提交
- commit `53672a0`：Day 08-27 第一个 C# 程序 - Hello World + 变量类型演示
- commit `d2ecf57`：Day 08-27 加分项 - ReadLine 输入名字打招呼
