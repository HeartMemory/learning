# Day 复盘 · 2026-08-27（周四 · Day 1 + Day 2 提前）

> 当天完成 Day 1 并提前完成 Day 2（弹性进度，当晚加餐）。

## 三句话复盘

### Day 1

1. **今天学了什么**：Console 的 Write / WriteLine 区别（Write 不换行，WriteLine 换行）；ReadLine / Read / ReadKey 区别（读一整行 / 读单个字符 / 读单个按键）；五种基础类型 int / double / bool / char / string；ReadKey 在 F5 调试下会报错，要用终端 `dotnet run` 运行。
2. **卡在哪了**：`Console.ReadKey()` 在 F5 调试运行时抛 InvalidOperationException（"console input has been redirected"，键盘输入被重定向），一度不知原因；改用终端运行后解决。
3. **明天第一步**：学运算符 / 表达式 / 类型转换，写计算器雏形（加减乘除函数）；LeetCode 第一题"两数之和"（用 C# 实现）。

### Day 2（提前版，当晚完成）

1. **今天学了什么**：算术 / 赋值 / 比较 / 逻辑运算符；`/` 整数除法丢小数、`%` 取余；类型转换（隐式 int→long→float→double、`Parse` 严格 vs `Convert` 宽容 vs `(int)` 截断、double→decimal 必须显式）；转义字符 `\n` `\t` `\"`；CS8600 警告是 null 可空问题。
2. **卡在哪了**：字符串拼接少写 `+` 报 CS1003 语法错误（`b "="` 应为 `b + "="`）；VS 调试控制台里 `Console.Write` 不显示输出，改用终端 `dotnet run` + `WriteLine` 解决。
3. **明天第一步**（弹性减量）：只剩 LeetCode 两数之和一题，其余明天不学新内容。

## 代码提交

- commit `53672a0`：第一个 C# 程序 - Hello World + 变量类型演示
- commit `d2ecf57`：加分项 - ReadLine 输入名字打招呼
- commit `1a144eb`：Day2 提前 - 计算器雏形（+ - * / % 五则运算）
