# Day 2 复盘（2026-08-28 周五）

> 注：Day 2 主线（运算符/类型转换/计算器雏形）已于 8/27 晚提前完成，今天补齐算法部分。

1. **今天学了什么**：LeetCode 第一题"两数之和"（暴力解法：双层循环 + `return new int[]`）；LeetCode 答题框架 `Solution` 类（类/对象/构造函数/`public`）；值类型 vs 引用类型（值类型不用 new，引用类型要 new）；C# 两阶段编译（"先用后定义"合法）；VS Code 自动保存。
2. **卡在哪了**：`int[i]` 误用类型名当数组变量；`=` 与 `==` 混淆；`j = i` 导致同一个数被用两次；漏 class 闭合大括号（CS1513）；缺程序入口（CS5001）；反复忘记保存导致文件没更新。
3. **明天第一步**（8/29 周六）：2 题数组简单题 + if/else、switch、三元表达式 + 计算器加分支逻辑（判断正负/大小）。

## 代码提交
- commit `50e4f3f`：Day 08-28 LeetCode 1 两数之和（暴力解法）AC
