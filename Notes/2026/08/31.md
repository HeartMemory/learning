# 复盘 · 2026-08-31（周一 · 数组/List + 第一周里程碑达成）

> Day6：数组详解 + List 基础 + TryParse 输入验证，主线目标「计算器循环菜单版」当天达成。

## 三句话复盘

1. **今天学了什么**：数组定长 vs List 可伸缩（Count 不是 Length）；Math.Max/Min 统计三件套；TryParse 输入验证循环（`while (!double.TryParse(...))`）；里程碑项目「计算器循环菜单版」跑通——历史记录（List<string>）、除 0 保护、取余、0 退出俱全；建立 `Projects/` 成品库结构（CSharp/Unity 分区）。
2. **卡在哪了**：修了三个 bug——①统计 min 初始值设 0 导致最小值恒为 0（53 题全负数的同款坑换马甲）；②历史记录复制粘贴后忘改运算符，减法记成加号；③空列表时 0÷0 输出 NaN。
3. **明天第一步**：9/1 机动日，可选数组中等题加餐或休整。

## 今日沉淀

- **成品 vs 练习分家**：`CSharpPractice/` 放学习过程，`Projects/CSharp/` 放可运行成品，同一项目在成品库里迭代
- **复制粘贴事故**：改模板时逐字检查「占位部分」（case 2/3/4 的运算符），这类 bug 测试跑不出来除非覆盖每个分支
- **TryParse 组合拳**：验证 + 重试三行搞定，`out _` 丢弃符处理「只要成败不要值」
- 成品设计意识：把「区分大小写与否」这类设计决策在输出里明示给用户

## 代码提交

- commit `65cb914`：计算器循环菜单版（历史记录+输入验证+除0保护）+ Projects 目录结构建立
- commit `3477cd4` / `b9e7a36` / `1f49c61` / `6d4b7d2`：笔记补全、复盘格式统一、Notes 重命名、提交信息去 Day 前缀
