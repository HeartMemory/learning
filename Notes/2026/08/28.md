# 复盘 · 2026-08-28（周五 · LeetCode 首题 + 分支语句）

> 8/27 的运算符/类型转换/计算器雏形已提前完成；分支语句于 8/28 下午提前完成。

## 三句话复盘

1. **今天学了什么**：LeetCode 第一题"两数之和"（暴力解法：双层循环 + `return new int[]`）；LeetCode 答题框架 `Solution` 类（类/对象/构造函数/`public`）；值类型 vs 引用类型；C# 两阶段编译；VS Code 自动保存；分支语句：if/else、switch（case 匹配规则、default 兜底、多 case 合并）、三元表达式；switch 菜单计算器 + 除 0 保护。
2. **卡在哪了**：`int[i]` 误用类型名；`=` 与 `==` 混淆；`j = i` 同数复用；漏 class 大括号（CS1513）；缺入口（CS5001）；`double answer` 缺分号；default 缺分号/break；switch 缺闭合大括号；if 文案写反。
3. **明天第一步**（8/29 周六）：只剩 2 题数组简单题（C# 主线已提前，弹性减量）。

## 代码提交

- commit `50e4f3f`：LeetCode 1 两数之和（暴力解法）AC
- commit `61d7486`：分支 - switch 菜单计算器 + 除 0 保护
