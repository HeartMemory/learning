# 复盘 · 2026-09-02（周三 · 两个成品 + 方法思维落地）

> 3h 档：上午字符串统计小工具做成品，下午方法学习 + 计算器 V2 函数化重构。

## 三句话复盘

1. **今天学了什么**：上午把统计逻辑升级为**字符串统计小工具**成品（四功能菜单式，进 Projects 成品库）；下午学方法（定义/参数/返回值/作用域），写完方法工具箱四件套，然后把计算器函数化重构出 **Calculator V2**（与 V1 并排对比）。
2. **卡在哪了**：①StringStats 两个**死循环**——倒序/回文的 while 里丢了指针推进语句（复制粘贴丢细节）；②V2 三个 bug：Mod 复制粘贴写成了除法（第三次！）、ZeroJudge 时序颠倒（先算后检查，打印新除数配旧无穷大）、范围检查装错了方法；③null 输入流导致无限刷屏——ReadLine 耗尽返回 null，TryParse 永远 false。
3. **明天第一步**：9/3 方法进阶：重载 / 递归入门 → 递归小练习（阶乘 / 斐波那契）。

## 今日沉淀

- **方法思维**：重复的验证代码消失；`ReadNumber(prompt)` 一个参数让一个方法服务 N 处调用；变量活在生成它的 `{}` 里，参数是入口、返回值是出口
- **原地填充的不变式**：每个指针每步必须恰好落一个值（死循环 = 违反了它）
- **职责分离直觉**：`ZeroJudge` 把关、`Divide` 专心算；范围检查归 `Choice`（菜单只有 7 项）不归 `ReadNumber`（数字无界限）——**限制来自调用场景，不来自数据本身**
- **null 防御**：`Console.ReadLine()` 可能返回 null（输入流耗尽），先接住判空再验证；已记入回访清单，学异常处理后来正规重构
- 错题本教训第三次应验：复制粘贴后逐字检查占位部分（Mod 运算符）
- HANDOVER 使命完成已删除：协作约定迁入 AI 记忆，环境备忘并入基础笔记

## 代码提交

- commit `e44965d`：字符串统计小工具 - 四功能菜单式成品
- commit `91d5508`：方法工具箱 - CountChar/IsPalindrome/Reverse/ReadNumber + 调用示例
- commit `4f676d6`：计算器 V2 方法化重构 + null 输入流防御
