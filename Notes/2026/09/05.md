# 复盘 · 2026-09-05（周六 · 🎓 Block 1 主线毕业：StringToolbox 收官）

> 3h 档里程碑日：字符串工具箱收官，全路径终验通过。

## 三句话复盘

1. **今天学了什么**：**StringToolbox 收官版**——五功能（单字符/子串计数、单词统计、查找替换、回文）全方法化，自建 `StringToolBox` 类 + 对象调用，`CountText` 重载 ×2，`ReadMenu`/`NullJudge` 5 次重试上限，null 防御全覆盖。Block 1 主线（字符串/方法）全部毕业。
2. **卡在哪了**：三个编译错误连环——①switch 各 case 共享作用域，`string a` 重名（修法：每个 case 套花括号）；②case 4 忘写 break 穿透到 case 5（CS0163）；③void 方法 `return 0`（CS0126）。另发现「假退出」：`n = 0; break;` 后循环顶部 n 被 ReadMenu 重赋值——把退出判断挪到 switch 外 `if(n == 0) break;` 才真退出。
3. **明天第一步**：9/6 复盘日——错题重做 + List 戒断练习 + 周记校准。

## 今日沉淀

- **switch 作用域**：各 case 共享作用域，套花括号即隔离；有空语句体才允许穿透
- **自建类 + 对象调用**：`new StringToolBox()` 凭直觉提前摸到面向对象的门——9/7 的 OOP 直接有体感了
- **超出规格的自我加码**：输入重试上限（5 次机会）设计，健壮性意识
- 成品库四件套达成：Calculator / CalculatorV2 / StringStats / StringToolbox

## 代码提交

- commit `1e1d82a`：StringToolbox 收官（五功能全方法化 / CountText 重载 / 重试上限 / null 防御）
