# 复盘 · 2026-09-08（周二 · 链表再进阶 + 静态成员 + 学生类封装收尾）

> Block 1 后半持续超前：09-09 静态成员、09-10 封装内容全部提前消化，83 收尾链表周。

## 三句话复盘

1. **今天学了什么**：LeetCode 206 反转链表（**头插法**：存后继 → 接结果头 → 前进）+ 141 环形链表（**快慢指针判圈**）双 AC；静态成员/const/readonly 三兄弟（TotalCount 计数、MaxAge 消灭魔法数字）；学生类**封装收尾**——`IReadOnlyList` 只读眼镜、Enroll 双卫语句、IsValidAge 静态校验源；83 去重（单指针原地跳过）收尾链表周。
2. **卡在哪了**：①141 自己把快慢指针「优化」成单指针查 head——**环入口不一定是 head**，卡死警告（及时回退，理解了 Floyd 判圈为何非快慢不可）；②Student 封装收尾时 `_courses` 忘初始化（null 定时炸弹）、`Couerses` 拼写、只读眼镜两行重复声明；③83 的 `while(cur.next != null)` 在空链表上崩（少判 cur 本身）。
3. **明天第一步**：9/9 主线剩余内容已提前消化，进入机动/加餐——可选：链表递归版重写（21 递归版）、13/125 的新解法重做，或 83 进阶变式（82 删除重复元素 II：重复全删不留一个，难度跳升）。

## 今日沉淀

- **引用类型地基课**：值类型变量装值、引用类型变量装地址——`cur = cur.next` 是换地址、`b.val = 99 影响 a` 是同对象别名、null = 空地址。四件往事全部闭环
- **头插法反转**：`next = cur.next; cur.next = dummy.next; dummy.next = cur;` 三行，先存后继再接线
- **快慢指针判圈**：环内相对速度 1 必追上，与入口位置无关——单指针「回 head」检测不了任意位置环（今日最贵弯路）
- **封装完整闭环**：private 字段（藏）→ 属性门卫（校验）→ `IReadOnlyList` 只读眼镜（受控暴露）→ 静态计数（类级数据）。口诀：字段藏起来、门卫站好岗、窗开多小自己定
- **`IsValidAge` 静态校验源**：规则只写一处（DRY），set 和构造函数共同调用；构造函数「合法才计数」
- 83 收尾：有序链表去重 = 单指针 + 相邻比较 + 跳过时 cur 不动

## 代码提交

- commit `2939cbe`：206 头插法 + 141 快慢指针
- commit `cf18931`：141 判圈优化（条件循环形态）
- commit `324f7f9`：21/203 优化落地（双 new 合并 / 悬尾断开挪位）
- commit `5377f6e`：学生类属性化（门卫 / 计算属性 / 静态计数）
- commit `89c8fa6` / `48ac020`：索引转正（累计 15 题 → 16 题）
- commit `a069b6e` / `324f7f9` 后续：Encapsulation/StaticMembers 测试插入 + 83 恢复学生版本

## 晚间加餐：静态统计与构造函数链（StaticMembers/Encapsulation 完成）

- **学生类静态统计收官**：`IsValidAge` 静态校验源（规则只写一处）→ Age 卫语句复用 → 构造函数 `if (IsValidAge(age)) _totalCount++`（非法学生不进户口本）
- **构造函数链**：`public Student() : this("张三", 18)` —— 无参版委托带参版，初始化逻辑只写一份（DRY），门卫/计数自动继承
- **默认参数 vs 链式二选一**：带默认参数的单构造已让 `new Student()` 天然可用；链式版只在「无参调用需要专属动作」时才值得；**两者并存 → CS0121 调用不明确**
- `Encapsulation` 收官终验：重复课/空课名被拦、课表只读眼镜（Count 可见 Add 不可见）、总数 1
- 83 恢复学生版本原样（含 TODO），本地测试全绿
