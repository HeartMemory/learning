# 复盘 · 2026-09-09（周三 · 链表周毕业 + 索引事故重建）

> 机动日收官：82 收尾链表周（6/6 毕业 🎓），21 递归版重写完成递归入门；主线内容已全部提前消化（学生类收尾 09-08 晚完成）。

## 三句话复盘

1. **今天学了什么**：LeetCode 82 删除排序链表中的重复元素 II AC（**哑节点 + prev/cur 状态机**：cur 探路扫完整个重复族、prev 锚在"最后一个确认保留的节点"上跨接，重复整族删除不留一个）——链表周 6 题（203/21/206/141/83/82）全部完成，毕业 🎓；21 合并两个有序链表**递归版重写**（终止条件 ×2 + 比较摘头 + 递归缩小规模），与迭代版对照测试全绿。
2. **卡在哪了**：①21 递归版的理解门槛——`list1.next = Merge(list1.next, list2)` 的返回值是"已经接好的子链头"，要**信任递归**直接接回当前头的 next，而迭代版需要 dummy + tail 手工接线，两种模型的对照是今天最大的收获；②索引漏题事故（13/485 丢失、82 重复）——根因是改索引没对照目录核对，已重建索引（commit `8a3cfb8`/`6ddd9e1`）并立规矩：**改索引必须对照目录列表核对数量**。
3. **明天第一步**：09-10 装 Unity Hub（编辑器数 GB，提前启动下载），学生类只剩收尾验收；顺手预习 09-11 继承入门（父类/子类/base）。

## 今日沉淀

- **82 的 prev/cur 状态机**：与 83 的差异——83 相等就跳过一个（cur 不动），82 要把重复族**整族删掉**（cur 扫完族、prev 不动跨接）。哑节点保命：头节点本身可能是重复族
- **递归版合并（21）**：递归三要素在链表上的落地——①终止条件：一方为 null 返回另一方；②递归步骤：小的头当结果头；③规模缩小：每次摘掉一个头。`return` 的就是"接好的子链头"
- **迭代 vs 递归对照**：迭代版显式管理 tail（手工接线），递归版隐式用调用栈接线——链表操作"信任递归返回值"是重要心智模型
- **链表周毕业总账**：六题覆盖四大套路——哑节点（203/21/82）、头插法（206）、快慢指针（141）、状态机（82）；递归版（21）作附加模型
- **索引规矩**：仓库索引（LeetCode/README.md）必须与目录一一对应，改动后按题号排序 + 难度逐题核对数量，杜绝漏题事故复发

## 代码提交

- commit `a0d284a`：82（哑节点 prev/cur 状态机）+ 21 递归版重写完成
- commit `8a3cfb8`：索引重建（补回 83、题号严格排序、难度逐题核对）
- commit `6ddd9e1`：补回 13/485，全表核对 19 题（17 简单 + 2 中等）与目录一一对应
- 本次收工：9/8、9/9 打卡表补行 + 本复盘 + CSharpPractice 索引补 07/08 + 21 递归版定稿
