# 复盘 · 2026-09-01（周二 · 机动日加餐：LeetCode ×2 + 字符串提前学）

> 主线已超前，机动日变成加餐日：LeetCode 88/118 + 字符串主题整体提前学完。

## 三句话复盘

1. **今天学了什么**：LeetCode 118 杨辉三角（List<List<int>> 嵌套实战）+ 88 合并两个有序数组（错误版 → 错题本 04 → 三指针重写 AC）；字符串主题提前学完——倒序两种实现、ASCII 字符统计（`c - 'a'`）、回文双指针判断。
2. **卡在哪了**：88 第一版双层循环「漏了第三条路」——nums1 有效元素全部更大时 j 走到头，nums2[i] 没人放凭空消失（`{3,0,0}` m=1 + `{1,2}` 输出 `3,3,0`）。换三指针模型（每步必放一个值）后 15 行一次通过，还自己发现「j<0 时 nums1 本来就在原位，无需收尾 while」的简化。
3. **明天第一步**：9/2 二选一——字符串统计小工具做成品 / 方法学习+计算器函数化重构。

## 今日沉淀

- **原地填充类题目的不变式**：每个指针每走一步必须恰好落一个值——多放、少放、跳过都是结构性 bug（错题本 04）
- **第三次靠自造反例救场**：题目给的测试一次都没兜住 bug，写完先自己当测试员
- **好的结构能消掉特判**：需要 `if (m==0)` 补丁时先想想换结构
- 字符串是**不可变**的：`s[i] = x` 编译错误，修改 = 创建新串；倒序靠 `char[]` 中转
- char 与 int 互通：`c - 'a'` 转下标、`(char)('a' + i)` 转回来；`char.IsLetter` 对所有语言字母返回 true，纯英文场景用范围检查防数组越界
- 仓库结构升级：`CSharpPractice/2026/09/01/Strings` 年/月/日三级；Notes 同步；Day 字样全面清除，提交格式改 `MM-DD: [主题]`

## 代码提交

- commit `accd007` / `047e456`：CSharpPractice 与 Notes 重构为 年/月/日 三级目录
- commit `10b6fe0`：LeetCode 88 错误版存档 + 118 杨辉三角
- commit `ada7111`：错题本记录 88
- commit `40703fb`：LeetCode 88 重写 - 三指针 O(m+n)
- commit `d66e273`：字符串三练习（倒序/统计/回文）
